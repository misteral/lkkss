<%= form_for(@type_pipe) do |f| %>
  <% if @type_pipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_pipe.errors.count, "error") %> prohibited this type_pipe from being saved:</h2>

      <ul>
      <% @type_pipe.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

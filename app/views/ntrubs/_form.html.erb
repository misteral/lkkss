<%= form_for(@ntrub) do |f| %>
  <% if @ntrub.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ntrub.errors.count, "error") %> prohibited this ntrub from being saved:</h2>

      <ul>
      <% @ntrub.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :shot_name %><br />
    <%= f.text_field :shot_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

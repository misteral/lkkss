<h1>Listing ntrubs</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Shot name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ntrubs.each do |ntrub| %>
  <tr>
    <td><%= ntrub.name %></td>
    <td><%= ntrub.shot_name %></td>
    <td><%= link_to 'Show', ntrub %></td>
    <td><%= link_to 'Edit', edit_ntrub_path(ntrub) %></td>
    <td><%= link_to 'Destroy', ntrub, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ntrub', new_ntrub_path %>

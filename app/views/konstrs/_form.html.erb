<%= form_for(@konstr) do |f| %>
  <% if @konstr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@konstr.errors.count, "error") %> prohibited this konstr from being saved:</h2>

      <ul>
      <% @konstr.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :shot_name %><br />
    <%= f.text_field :shot_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

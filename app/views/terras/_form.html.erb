<%= form_for(@terra) do |f| %>
  <% if @terra.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@terra.errors.count, "error") %> prohibited this terra from being saved:</h2>

      <ul>
      <% @terra.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

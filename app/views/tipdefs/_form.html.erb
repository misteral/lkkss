<%= form_for(@tipdef) do |f| %>
  <% if @tipdef.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipdef.errors.count, "error") %> prohibited this tipdef from being saved:</h2>

      <ul>
      <% @tipdef.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

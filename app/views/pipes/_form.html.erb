<%= form_for(@pipe) do |f| %>
  <% if @pipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pipe.errors.count, "error") %> prohibited this pipe from being saved:</h2>

      <ul>
      <% @pipe.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :notation %><br />
    <%= f.text_field :notation %>
  </div>
  <div class="field">
    <%= f.label :passport_pipe_id %><br />
    <%= f.number_field :passport_pipe_id %>
  </div>
  <div class="field">
    <%= f.label :type_pipe_id %><br />
    <%= f.collection_select :type_pipe_id, TypePipe.all, :id, :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

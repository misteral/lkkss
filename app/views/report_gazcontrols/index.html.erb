<h1>Listing report_gazcontrols</h1>

<table>
  <tr>
    <th>Date start</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @report_gazcontrols.each do |report_gazcontrol| %>
  <tr>
    <td><%= report_gazcontrol.date_start %></td>
    <td><%= link_to 'Show', report_gazcontrol %></td>
    <td><%= link_to 'Edit', edit_report_gazcontrol_path(report_gazcontrol) %></td>
    <td><%= link_to 'Destroy', report_gazcontrol, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Report gazcontrol', new_report_gazcontrol_path %>

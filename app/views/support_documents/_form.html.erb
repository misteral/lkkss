<%= form_for(@support_document) do |f| %>
  <% if @support_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@support_document.errors.count, "error") %> prohibited this support_document from being saved:</h2>

      <ul>
      <% @support_document.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :name_product %><br />
    <%= f.text_field :name_product %>
  </div>
  <div class="field">
    <%= f.label :date_issue %><br />
    <%= f.datetime_select :date_issue %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

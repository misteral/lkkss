<%= form_for(@ty) do |f| %>
  <% if @ty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ty.errors.count, "error") %> prohibited this ty from being saved:</h2>

      <ul>
      <% @ty.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Наименование" %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
